{
    "version": 2,
    "name": "motorspa-landing",
    "buildCommand": "cd intro && echo 'Ready'",
    "outputDirectory": "intro",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/intro/$1"
        }
    ]
}